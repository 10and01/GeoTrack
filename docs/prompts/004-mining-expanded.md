# Prompt 004 · 停留点、热点与时段模式（工程版）

## 提示词

```text
请阅读 docs/specs/004-mining.md 和 Spec 002 的数据契约，在 jobs/geotrack_core.py 中实现可解释的挖掘模块。

停留点：Haversine 米制距离，半径 200m，最短持续 20 分钟，时间断点 30 分钟；输出用户、轨迹、起止时间、持续秒数、中心和覆盖半径。

热点：对停留中心执行 DBSCAN，默认 eps=500m、minPts=3；输出中心、范围、访问次数、独立用户、平均停留、峰值小时、工作日比例、算法名。模式：按用户-日期构造轨迹数、距离、时长、平均速度、停留、高峰、夜间、工作日比例和 24 小时分布，KMeans 默认 k=3、seed=42。

保留 cluster_id、中心特征和样本数；中文标签只能依据中心特征自动说明，不能当作人工真值。参数集中定义并可由批处理覆盖。为已知距离、固定停留、时间断点、两个 DBSCAN 热点和小样本 KMeans 编写测试。
```

## 提交

输出参数、样例结果、失败边界和测试记录；提交信息使用 `feat: implement stay point and hotspot mining`。
